package com.example.myapplication.ui.settings

import androidx.lifecycle.ViewModel

class SettingsViewModel : ViewModel() {
    // Add LiveData and methods here as settings functionality is developed.
    // For example:
    // private val _indoorModeEnabled = MutableLiveData<Boolean>(false)
    // val indoorModeEnabled: LiveData<Boolean> = _indoorModeEnabled

    // fun setIndoorMode(enabled: Boolean) {
    //     _indoorModeEnabled.value = enabled
    // }
}